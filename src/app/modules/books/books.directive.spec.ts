import { BooksDirective } from './books.directive';

describe('BooksDirective', () => {
  it('should create an instance', () => {
    const directive = new BooksDirective();
    expect(directive).toBeTruthy();
  });
});
